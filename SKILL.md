---
name: quad
description: >-
  Antigravity 多智能体协同编排技能 (Quad)。当面对跨模块开发、大范围重构、深度调研或需要独立验证的复杂任务时，协调主控与四类专职子智能体（侦察员 / 攻坚员 / 审查员 / 验收员，统一采用 flash 模型，基于同一工作区单分支推进）进行任务拆解、并行侦察、单点攻坚、独立质量验证与用户视角验收。
---

# Antigravity 多智能体协同编排技能 (Quad)

本技能定义主智能体如何协调多个专职子智能体（Subagents）进行大粒度任务的拆解、分工、实现与**独立验证**。

---

## 零、这份技能为什么这样写（读模板前先读这段）

全部智能体都跑 `flash`。历史上本技能出现过一次严重失效：某个 WP 收口时审查员一次性漏过 15 个显性缺陷并给出「建议准予正式收口」。事后定位到四个**结构性**原因，全部与模型态度无关：

1. **审查员的参照系是 diff，不是契约。** 它被要求「审计攻坚员改了什么」，于是它的期望值只能从被审代码的现有行为倒推。倒推出来的断言永远通过。
2. **攻坚员的自评进了审查员的上下文。**「已完成、已自测通过」这类话术一旦出现在上下文里，就等于摆了一个现成答案，flash 会顺着它续写，而不是独立重新推导。
3. **审查员同时是测试作者、修复者和签收者。** 目标被写成「实现 100% 测试通过」，那么写一个必然通过的测试就是最省力的最优解。这是目标函数问题，不是偷懒。
4. **审查员被禁止跑全量测试，只能跑自己新建的那个文件。** 于是它报的「全绿」只意味着「我写的测试通过了」，信息量为零。前端那批缺陷本来要靠全量渲染测试和 Playwright 才能暴露。

由此确立本技能的三条设计准则：

- **flash 不执行形容词，只执行动词。**「对抗性推演」「穷尽排查」等于没写。质量要求必须落成**可逐条勾选的清单**和**可直接复制执行的命令**。
- **独立性来自输入隔离，不来自身份设定或情绪施压。** 不要给审查员打鸡血（「找不出问题就是你失职」会让它为凑数交出一堆无价值边界 case，真缺陷反被噪音淹没）。要做的是**把污染源从它的输入里删掉**。
- **判断力留在主控和人类侧，执行力交给 flash。** 期望数值必须来自任务书原文、数学恒等式或用户，**严禁任何智能体自己编造验收数值**。

---

## 一、核心原则（Core Principles）

### 1. 统一 flash 模型与显式推理强度分级
- 所有子任务派发统一 `Model: "flash"`，`Workspace: "inherit"`。
- 通过 Prompt 显式约束思维链深度：
  - **侦察员 (Scout)**：低推理（直出），聚焦模式匹配与定位。
  - **攻坚员 (Worker)**：高推理（深度推演），推演架构不变量、并发安全、极端边界。
  - **审查员 (Reviewer)**：高推理（深度推演），逐条走缺陷清单，独立推导期望值。
  - **验收员 (Acceptance)**：低到中推理，照场景卡核对证据，复核算术。

### 2. 单分支无缝推进与单文件独占防踩踏
- 禁止创建额外 Git 分支或多 worktree。
- 主控必须为并行的 Worker 分配**互不重叠的具体文件**所有权，精确到文件，严禁以目录为界。
- **公共汇总文件剥离**：`docs/CHANGELOG.md`、`README.md`、全局契约索引等严禁划入任何 Worker 的独占列表，也严禁作为串行化的冲突证据。由主控在收敛交付时统一追加。

### 3. 防递归与 Git 单写者
- 所有子智能体均为**叶子节点**，严禁再次调用 `invoke_subagent`。
- 子智能体**终生只读访问 Git**，严禁 `git add` / `git commit`。主控是唯一 Git 写者，根绝 `.git/index.lock` 冲突。

### 4. 物理产物隔离与无锁并发
- 并发编译或测试时强制注入隔离构建缓存：`CARGO_TARGET_DIR=target/subagents/<role>-<id>`。
- 并发测试使用动态空闲端口（port 0）或 Mock，严禁争抢固定端口。
- Reviewer 的独占测试文件全路径必须互相隔离。

### 5. 精炼结构化回报（Context Budgeting）
- 子智能体只汇报关键事实、文件路径、命令原始输出摘要，严禁大段复制代码刷屏。

### 6. 并发批次编排（文件交集判定）
- 主控拆解任务后输出**二维批次矩阵**，Batch 内文件互斥的 Worker 在**单次** `invoke_subagent` 调用中同时拉起：
  ```markdown
  ### 任务编排规划
  - 【Batch 1 (并发攻坚)】：
    * Worker 1 (Task X) ➔ 独占文件：[`crates/foo/src/a.rs`]
    * Worker 2 (Task Y) ➔ 独占文件：[`crates/bar/src/c.rs`]
  - 【Batch 2 (依赖收敛)】：
    * Worker 3 (Task Z) ➔ 独占文件：[`crates/foo/src/mod.rs`]
      - 【不可并发证据】：依赖 Task X 导出的未落盘 struct。
  ```
- **判定算法**：提取每个 Task 的核心业务源码文件集（剥离公共汇总文档），两两求交集；交集为空则必须归入同一 Batch 并发派发。
- **仅两种合法的串行证据**：① 核心源码文件全路径交集非空且无法拆分；② 同语言物理编译符号强依赖（B 依赖 A 新建且未落盘的符号，不先写 A 则 B 语法检查必红）。
- **非法串行借口（一律驳回打平为并发）**：以「每个 Task 需单独 commit」为由；以 `CHANGELOG.md` 等公共文档写冲突为由；以「端到端业务先后」为由（单测隔离即可并发）；以「同属一个 crate / 目录」为由。

### 7. 输入隔离铁律（防审查沦为盖章的核心机制）
- **Worker 的自评严禁进入 Reviewer 的 Prompt。** 主控向 Reviewer 转述时只传递：改了哪些文件、每个文件的改动点。「已完成」「已自测通过」「编译通过」「请签收」这类话术一律过滤掉。
- **Reviewer 的锚点必须是任务书验收项，不是 diff。** Prompt 里的【本次需验证的需求】必须抄任务书原文并带**具体数值**；任务书没有具体数值时向用户索取，**主控不得自己编**。
- **Acceptance 层隔离源码。** 验收员统一用 `TypeName: "research"`（该类型物理上没有写权限），并在 Prompt 中明令禁止读取 `frontend/` `go/` `crates/` `sql/` 下任何源码。
  注意：**禁止读源码这条只是 Prompt 约束，平台不强制**。所以验收员的报告里必须能看出它的结论来自证据文件（引用了截图文件名、observed.json 里的数值）；一旦它开始解释「代码里是这样处理的」，说明它读了源码，这次验收即失效，需重派。

### 8. 审查阻断与职责剥离
- **严禁主控私自越权签收**：Worker 汇报后，主控的下一步唯一合法动作是拉起专属 Reviewer，**绝对禁止**直接向用户宣告交付或自行 commit。
- **1:1 结对即发，零排队**：任意 Worker 交付的瞬间立即拉起其专属 Reviewer，不得挂起等待同批次其他 Worker，也不得等待其他在跑的 Reviewer。
- **Reviewer 只报告，不修复**：发现缺陷时写出能复现的失败测试并**保留失败状态**，禁止修改任何非测试文件让它变绿。修复退回 Worker，Reviewer 下一轮复核。
- **签收权归主控**：Reviewer 不输出「批准提交」「准予收口」，它只交发现报告。是否提交由主控依据门禁判断。

### 9. 物理事实核验（主控侧硬门禁）
规则只约束子智能体、主控侧没有核验动作，制度就是空文。因此主控在接受任何 Reviewer 报告前**必须**执行：
```bash
git status --short
git diff --stat
```
核验三件事，任一不符即驳回并要求补齐：
1. Reviewer 声明的独占测试文件**真实存在**（`??` 新建 或 `M` 修改）；报告说写了测试而工作区零测试文件落盘 → 驳回。
2. **非测试文件没有被 Reviewer 动过**。若有，说明它改了实现来迎合自己的测试 → 违反「只报告不修复」→ 驳回。
3. 报告里声称的文件路径与 `git status` 显示的**逐条一致**。

> 本核验**不计入**原则 10 的两步熔断命令预算。它是不需要读源码的事实核查，是主控唯一能行使的独立判断，不得省略。

### 10. 侦查阻断与两步熔断
- **静态调研 vs 动态探针的严格隔离**：
  - 纯只读代码/文档调研 → **锁死 `TypeName: "research"`**，严禁赋予 `self` 权限；
  - 凡需运行命令（`playwright`、`cargo test`、`pytest`）或落地证据日志的**动态复现**，绝不是 Scout，必须作为 **Worker** 编入 Batch 1，并划定独占证据落盘目录（如 `.data/investigation/...`）。
- **两步熔断**：主控在主会话中严禁 `view_file` 阅读任何 >50 行的业务源码；最多执行 2 次轻量目录/状态确认。需要深入阅读代码或排查调用链时，第 1 步即触发熔断，必须派发 Scout。
- **豁免**：原则 9 的 `git status` / `git diff --stat` 核验，以及最终收敛的全量门禁，不计入此预算。

---

## 二、角色分工与职责隔离

| 角色 | TypeName | 推理强度 | 核心职责 | 行为禁区 |
| :--- | :--- | :--- | :--- | :--- |
| **主控协调者** | （主会话） | 随全局 | 需求对齐、文件交集检测、批次编排、**过滤 Worker 自评**、**物理事实核验**、收敛分步原子提交 | 禁止未经独立验证签收；禁止把 Worker 自评转给 Reviewer；禁止自己编造验收数值；禁止在主会话详读业务源码 |
| **侦察员 (Scout)** | `research`（锁死） | 低（直出） | 只读定位符号、调用链、任务书与契约 | 严禁修改任何文件；严禁用 `self` 冒充；严禁跑命令做动态复现 |
| **攻坚员 (Worker)** | `self` | 高（深推） | 实现功能 + 写覆盖正常路径的基础单测；或作为动态探针跑测抓证 | 禁止越权改未授权文件；严禁改 CHANGELOG/README；严禁 Git 写；**禁止在回复中写「已完成/请签收」等结论话术** |
| **审查员 (Reviewer)** | `code-reviewer` | 高（深推） | 依据**任务书验收项**独立验证，逐条走缺陷清单，落地边界与异常路径测试，输出发现报告 | **禁止改任何非测试文件**；禁止从被审代码倒推期望值；严禁 Git 写；禁止输出「批准提交」 |
| **验收员 (Acceptance)** | `research`（锁死） | 低到中 | 按场景卡核对运行证据，复核每个金额的算术，回答「用户看到的对不对」 | **禁止读取任何源码**（读了本次验收即失效）；禁止写文件；禁止输出「批准收口」 |

---

## 三、标准执行流程（六阶段）

### 第一阶段：任务分诊与批次编排
1. **职责分离**：无论任务大小，一律遵循「实现与验证分离」，严禁主控单干并直接结项。
2. 按原则 6 的文件交集算法输出二维批次矩阵，对每个后置串行的 Task 手写【不可并发证据】，并对照非法借口清单自查。
3. **验收项前置**：编排时即确定每个 Task 的验收项与**具体期望数值**。任务书里没有具体数值的，此时就向用户索取——不要等到审查阶段才发现无从判断。

### 第二阶段：侦查与调研
1. **必须派 Scout 的场景**：跨模块查找符号/调用链；查阅长篇任务书、架构设计、PRD、DDL 契约；触发两步熔断时。
2. **唯一免派条件**：用户已明确指定需改动的具体文件全路径与改动行。
3. Scout 输出控制在 300 字内的精炼结论 + 文件列表 + 关键依赖。

### 第三阶段：单点攻坚
1. 派发 Batch 1 时，在**单次** `invoke_subagent` 调用的 `Subagents` 列表中同时填入该批次全部 Worker，严禁拆成多次串行下发。
2. 每个 Worker 的 Prompt 严密划定：独占文件全路径、公共文件与 Git 写禁区、架构不变量约束。
3. Worker 写覆盖**正常路径**的基础单测并确保编译通过即可交付；边界与异常路径留给 Reviewer 独立验证（避免 Reviewer 无事可做而照抄 Happy Path）。
4. Worker 交付摘要只含事实：改了哪些文件、改动点、命令原始输出、自己不确定的地方。

### 第四阶段：独立质量验证
1. **1:1 结对即发**：收到任意 Worker 交付的瞬间无条件立即拉起专属 Reviewer，保持全并发流水线。
2. **主控组装 Reviewer Prompt 时必做三件事**：
   - **过滤**：删掉 Worker 自评里的一切结论话术，只留文件与改动点；
   - **填验收项**：抄任务书原文，带具体输入与期望输出数值；
   - **预填命令**：把完整验证命令写进 Prompt（含 `CARGO_TARGET_DIR` 隔离、动态端口）。留给 Reviewer 自选，它必然选最小的那个。
3. **验证范围下限**（主控预填时必须满足）：
   - Rust / Go：定向单测 + 定向 lint（`cargo test -p <pkg> --test <target>`、`go test -run <TestName> ./path/...`）。
   - **前端任务必须包含 `cd frontend && npm test` 全量渲染测试**——只跑新建的那一个测试文件不构成有效验证。
   - **UI 交互任务必须包含 Playwright 真实浏览器脚本并留截图证据**。SSR 渲染不报错 ≠ 功能可用；弹窗能否关闭、按键步进精度、断线重连、组件卸载清理这几类缺陷，渲染测试一律测不到。
4. **仍然保留的防撞锁约束**：子智能体严禁 `make test` / `make lint` / `cargo test --workspace` 等全仓聚合命令（全量门禁由主控在收敛阶段独占串行执行）；严禁在测试用例内通过子进程反向调用 `cargo` / `make` / `go`。
5. **收到报告后执行原则 9 的物理事实核验**，通过才算这一环闭合。报告缺项（验收项逐条判定、缺陷清单逐条作答、命令原始输出）一律驳回。

### 第五阶段：用户视角独立验收（涉及用户可见行为的任务强制）
前四阶段全部通过仍然漏缺陷，是因为审查员**手里没有那个信息**——它读代码和跑单测，看不见「弹窗没有取消按钮」。这一层补的是这个能力缺口，不是态度缺口。

1. **触发条件**：任务改动了任何用户能看到或能操作的东西（页面、交互、金额显示、状态流转）。纯内部重构、纯数据面改动可跳过并注明理由。
2. **先产证据**：派一个 Worker 作为动态探针，用 Playwright 驱动**真实浏览器打真实后端**（禁止 mock 接口——mock 出来的数字永远自洽，测不出金额算错），逐步截图并落盘到独占目录：
   ```
   .data/investigation/<tag>/
     01-*.png … NN-*.png    每步截图
     observed.json           界面上抓下来的数值
     console.log             浏览器控制台
     network.har             接口调用时间线
   ```
3. **再派验收员**（模板四）：`TypeName: "research"`，输入只有场景卡 + 证据目录，明令禁止读源码。
4. **场景卡由主控生成**，每步必须写「用户做什么 → 界面上应该出现什么（含具体数值）」。金额类期望必须给出算式，例如：
   `50 股 × 10.25¢ = 5.125 → 进位到分 = 冻结 $5.13；可用 94.87 + 冻结 5.13 = 100.00 必须闭合`
5. **验收员的核心产出是算术复核**：代码和测试可能一起错，算术不会。屏幕上三个数闭不上，就是缺陷，无需理解实现。

### 第六阶段：收敛交付
1. 当前 Batch 的全部 Worker、Reviewer（及适用的 Acceptance）全部闭合后，主控作为唯一 Git 写者，独占串行执行一次全量门禁（`make lint`、`make test`）。
2. **分步原子提交**：按 Task 顺序逐一暂存该 Task 的业务文件 + 测试文件，在 `docs/CHANGELOG.md` 追加该 Task 变更说明，然后 `git add <task_files> docs/CHANGELOG.md && git commit -m "WP-XX-TXX: ..."`。依次推进，保持 Git 历史的 Task 粒度原子性。
3. **禁止合并提交**多个 Task 的改动；禁止在任何 Reviewer 未闭合、或原则 9 核验未通过时提交。

---

## 四、失效自查（交付前主控逐条过一遍）

- [ ] Reviewer 的 Prompt 里有没有残留 Worker 的自评话术？
- [ ] Reviewer 的 Prompt 里的验收项带具体数值吗？还是只有抽象描述？
- [ ] `git status --short` 核验做了吗？非测试文件有没有被 Reviewer 动过？
- [ ] 前端任务跑了 `npm test` 全量吗？还是只跑了新建的那一个文件？
- [ ] UI 交互任务有真实浏览器截图证据吗？
- [ ] 有 Reviewer 报告里写着「未发现问题」但缺陷清单大片留空吗？
- [ ] 涉及用户可见行为的任务，第五阶段验收做了吗？跳过的理由写了吗？
- [ ] 有没有任何一处期望数值是智能体自己编的、而不是来自任务书或用户？

---

## 快速参考

- 详细中文 Prompt 模板（含四类角色模板与填写须知）：见 [prompt_templates.md](references/prompt_templates.md)
- Antigravity API 调度速查：见 [antigravity_guide.md](references/antigravity_guide.md)
